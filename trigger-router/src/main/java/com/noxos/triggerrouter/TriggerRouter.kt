package com.noxos.triggerrouter

/**
 * Placeholder for the trigger/routing module.
 *
 * This module will eventually detect untrusted content (files, network
 * payloads, etc.) and route it into a disposable Microdroid VM (via
 * Android's pKVM hypervisor / AVF) for isolated parsing, then tear the VM
 * down. Not yet implemented — see the project roadmap, phases P6-P10.
 */
object TriggerRouter
